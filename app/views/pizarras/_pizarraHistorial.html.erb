    <% if fecha == DateTime.now.to_date %>
        <% @avisos = @pizarra.avisos %>
        <% @avisos.publicados.each do |aviso| %>
          <%= render :partial => "avisos/note", 
            :locals => {:aviso => aviso } %>

         <% end %>
    <%else%> 
       <% if fecha < DateTime.now.to_date %>
       <% @avisos.each do |aviso| %>
        <%= render :partial => "avisos/note", 
            :locals => {:aviso => aviso } %>
       <%end%> 
      <% end %>
    <%end%>








